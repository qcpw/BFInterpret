#include <stdio.h>
#include <string.h>

int directMatch(const char* regex, const char* input, int regexSize, int inputSize){
  for(ii = 0; ii < inputSize - regexSize; ii++){
    if(strcmp(regex, input[]))
  }
}
